<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EPCalipersWinUI3.Views.MeanRateIntervalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EPCalipersWinUI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    KeyUp="Page_KeyUp" >

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"
                   Text="{x:Bind ViewModel.Title}" Margin="15" />
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" x:Uid="Measurement" />
        <TextBlock x:Name="TotalInterval" Margin="10,0,0,0"
                   Text="{x:Bind ViewModel.TotalInterval, Mode=OneWay}"/>
        <NumberBox x:Name="NumberOfIntervalsBox" Margin="10,0,0,0" x:Uid="NumberOfIntervals"
            Value="{x:Bind ViewModel.NumberOfIntervals, Mode=TwoWay}" 
            SpinButtonPlacementMode="Inline" ValidationMode="InvalidInputOverwritten"
            Minimum="1"
            Maximum="10" />
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,5,0,0" x:Uid="Results" />
        <TextBlock x:Name="MeanRate" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="10,0,0,0"
                   Text="{x:Bind ViewModel.MeanRate, Mode=OneWay}" Visibility="{x:Bind ViewModel.RateVisibility}"/>
        <TextBlock x:Name="MeanInterval" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="10,0,0,0"
                   Text="{x:Bind ViewModel.MeanInterval, Mode=OneWay}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="DoneButton" Grid.Column="1" Margin="2,12,0,0" Click="MeanRateIntervalViewCancel_Click"
                    HorizontalAlignment="Right" x:Uid="ButtonDone" />
        </Grid>
    </StackPanel>
</Page>
