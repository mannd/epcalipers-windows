<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EPCalipersWinUI3.Views.MeasureIntervalView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EPCalipersWinUI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    KeyUp="Page_KeyUp" >

    <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" x:Uid="QTInterval" HorizontalAlignment="Center"
                   Margin="10"/>
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" x:Uid="Measurement" />
        <TextBlock x:Name="Interval" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="10,5,0,0"
                   Text="{x:Bind ViewModel.TotalInterval, Mode=OneWay}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Margin="2,12,0,0" Click="Done_Click"
                    HorizontalAlignment="Right" x:Uid="ButtonDone" />
        </Grid>
    </StackPanel>
</Page>
