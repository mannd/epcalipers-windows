<Page
    x:Class="EPCalipersWinUI3.Views.HelpWebViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EPCalipersWinUI3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="ContentArea" MinWidth="400" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Margin="10" Click="BackButton_Click" HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon>Back</SymbolIcon>
                <TextBlock Margin="10" x:Uid="ButtonBack" />
            </StackPanel>
        </Button>
        <TextBlock Grid.Row="0" x:Uid="HelpPageTitle" Margin="10"
                   HorizontalAlignment="Center" Style="{StaticResource TitleLargeTextBlockStyle}" />

        <WebView2 DefaultBackgroundColor="Transparent"
            x:Name="WebView" Grid.Row="1" />

        <StackPanel Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
            <TextBlock x:Uid="WebView_Loading" />
        </StackPanel>
    </Grid>
</Page>
